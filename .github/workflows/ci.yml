name: CI

on:
  push:
    branches: ["*"]  # Run on all branches
  pull_request:
    branches: [main]

jobs:
  test-linux:
    runs-on: [self-hosted, Linux, X64]
    env:
      VIRTUAL_ENV: ~/.venvs/voxtype
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: openvip-dev/sdks
          token: ${{ secrets.OPENVIP_SDK_TOKEN }}
          path: openvip-sdks

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install system deps (Xvfb for tray tests)
        run: |
          which xvfb-run || sudo apt-get install -y xvfb

      - name: Set up Python and install deps
        run: |
          test -d $VIRTUAL_ENV || uv venv --python 3.11 $VIRTUAL_ENV
          uv pip install -e ./openvip-sdks/python
          uv pip install --no-sources -e ".[dev]"

      - name: Run linter
        run: ruff check src/ tests/

      - name: Run tests with coverage
        run: xvfb-run pytest tests/ -v -m '' --cov=voxtype --cov-report=term-missing

  test-macos:
    runs-on: [self-hosted, macOS, ARM64]
    env:
      VIRTUAL_ENV: ~/.venvs/voxtype
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: openvip-dev/sdks
          token: ${{ secrets.OPENVIP_SDK_TOKEN }}
          path: openvip-sdks

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python and install deps
        run: |
          test -d $VIRTUAL_ENV || uv venv --python 3.11 $VIRTUAL_ENV
          uv pip install -e ./openvip-sdks/python
          uv pip install --no-sources -e ".[dev]"

      - name: Run linter
        run: ruff check src/ tests/

      - name: Run tests
        run: pytest tests/ -v -m ''

  typecheck:
    runs-on: [self-hosted, Linux, X64]
    env:
      VIRTUAL_ENV: ~/.venvs/voxtype
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: openvip-dev/sdks
          token: ${{ secrets.OPENVIP_SDK_TOKEN }}
          path: openvip-sdks

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python and install deps
        run: |
          test -d $VIRTUAL_ENV || uv venv --python 3.11 $VIRTUAL_ENV
          uv pip install -e ./openvip-sdks/python
          uv pip install --no-sources -e ".[dev]"

      - name: Run mypy
        run: mypy src/
